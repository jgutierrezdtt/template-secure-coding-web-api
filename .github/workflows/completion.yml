name: Completion

on:
  workflow_dispatch:

jobs:
  completion:
    uses: jgutierrezdtt/security-tutorial-base/.github/workflows/reusable-completion.yml@main
    with:
      tutorial-name: Secure Coding Web API
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
